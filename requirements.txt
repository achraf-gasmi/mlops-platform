# ─────────────────────────────────────────────────────────────────────────────
# Picnic ML Platform — Python Dependencies
# Pinned for reproducibility; update with `pip-compile` when upgrading.
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# ML core
scikit-learn==1.4.2
xgboost==2.0.3
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0

# Time-series forecasting
# prophet requires pystan; install via conda on M1/Windows if pip fails:
#   conda install -c conda-forge prophet
prophet==1.1.5

# Experiment tracking
mlflow==2.13.0

# HTTP client (used by monitoring sidecar)
requests==2.31.0

# Testing
pytest==8.2.0
httpx==0.27.0          # async test client for FastAPI
